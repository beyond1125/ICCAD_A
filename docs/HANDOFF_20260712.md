# 交接進度(2026-07-12)

> 本文件記錄 2026-07-11 深夜~07-12 這輪工作的完整狀態,供接手者
> (或平行 session)快速上手。主分支:`integrate/eval-and-gates`。

## TL;DR

- **P1-8 功能恆定分析已完成並全面驗證**(commit `5880dc7`,已推 origin),
  `docs/PLAN_QA_fixes.md` 的 P0/P1 項全數落地,只剩 P2-9 / P2-6 / P2-7(可選)。
- **時序 BLIF 橋接 + scleanup/pdr 已完成並合併**(`9e94514`,合併
  `53d5e48`)—— check_const 現在有真正的時序證明通道,pdr 反例可給出
  確定的 NOT CONSTANT。
- **check_answers 提取誤報修復已完成並合併**(`e919a95`,合併 `439450a`)。
- **合併後驗證通過**(07-12):parser 重建、check_answers selftest OK、
  test02 金絲雀 OK、**check_results 40/40 無硬性違規**,已推 origin。
  shaw worktree/分支已棄置刪除。
- **P2-9 分析 cone 語意對齊已完成**(07-12 下午):五個 cone 分析工具改
  組合語意(A21.2),1760 組 tool vs oracle 差分全對、LLM 回歸五案零
  WRONG、40/40 維持;A21.5 單執行緒不變量入 agent-runtime。**API 已切到
  openai/gpt-4o-mini**(使用者換了有 quota 的 key,canary 過)。
- **P2-6 floating-port 容錯已完成**(07-12 傍晚):4 fixture 常駐測試
  `tests/unit_tests/test_floating_ports.py` 全過;修復 check_const 對
  undriven 網的裁決(短路回 CONSTANT 0 + floating 註記,對齊 sim/ABC
  tie-0 慣例)。
- **全量 sweep 收官已完成**(07-12 晚,gpt-4o-mini,乾淨輸出起跑):
  run_all_llm **40/40 OK**、check_results **40/40 無硬性違規**、
  eval_harness **40/40 PASS+EQUIV**、差分 40/40。穩定性重跑
  (test18/20/37 × 3 輪)變換側完全確定(gate 數逐輪相同、全 PASS)。
  唯一穩定丟分缺口:**signal_equiv 空口宣稱**(6 筆 WRONG,詳見 §7)
  —— 修法即 keller 任務。
- **下一步:keller signal-equiv 工具(scorr 路線)+ planner 對
  signal_equiv 題型加 anti-fabrication 閘門**(見 §4、§7)。

## 1. P1-8:功能恆定分析(Q&A A21.1)— 已入庫 `5880dc7`

**新能力**:
- `check_const(net)` 工具:「輸出 X 是否恆 0/恆 1」的功能性判定 ——
  隨機時序模擬(DFF 初始態 0)找非恆定見證 → 找不到再以 cone BLIF +
  ABC `strash; sat` 證明,疊加 DFF-init-0 定點迭代;verdict 以
  `_loaded_filepath` 為鍵快取。
- `const_propagate` 新增 `semantics='functional'`:const 報告/化簡題
  依官方語意認「可證明恆定」的輸入,不只 1'b0/1'b1 綁定。
- C++ 新 action:`report_stuck_inputs` / `sim_consts` / `write_cone_blifs` /
  `list_dffs` / `tie_nets_const`。
- 時間預算:check_const 110s、functional 批次 80s、單次 SAT 20s
  (逾時不算證明;見 `.claude/rules/agent-runtime.md` 階層表)。

**效能修復(重要)**:`run_random_sim` 原以逐拍 `unordered_map` 存網值,
test39(5.6MB)單次掃描 ~90s,整案 900s TIMEOUT。改為稠密槽位編譯
(節點一次性編號 + 扁平閘程式)後 ~5s,**新舊輸出逐位元組相同**
(test26/test39 雙重比對)。test39 現為 481s OK。

**驗證紀錄**(全部通過):
| 項目 | 結果 |
|---|---|
| test31 Q8 vs oracle `always_const(n16)` | 一致(皆非恆定,agent 答 No) |
| test32/36/38/39/40 重跑 | 全 OK;eval_harness 全 PASS+EQUIV |
| `check_results.py --all` | **40/40 無硬性違規** |
| `check_answers` 全部已跑用例 | WRONG=0、DIVERGENT=0 |
| test39 OR stuck=1 候選抽查 4 網 | oracle 皆 None,與「0 可證明+10 未證明」的誠實回答一致 |

## 2. 時序 BLIF 橋接 + scleanup/pdr — 已完成並合併(`9e94514` → `53d5e48`)

來自分支 `claude/pensive-leakey-a0740c`。動了 `graph.cpp`/`main.cpp`/
`engine.py`/`tool_spec.py`;合併後已重建 parser 並驗證(§4-1)。

**新能力**:
- C++ 新 action `write_seq_blif`:時序 BLIF 匯出(`.latch D Q 0`、真實
  PI/PO;`--expose` 可把內部網拉成 PO 或建反相 miter)。latch 數與
  `list_dffs` 對帳通過:test26 205/205、test39 16050(+96 重複 Q 去重)。
- `check_const` 在「模擬找見證」之後、「手工 DFF 定點」之前插入時序證明
  通道:先 `scleanup`(~0.7s@16k latches;PO 收斂為字面常數即為證明)→
  預算允許再 `pdr -T`。**pdr 反例(asserted in frame N)是真實時序見證,
  可給出確定的 NOT CONSTANT** —— 這是 flop-cut SAT 做不到的(它只能證
  恆定,不能證非恆定)。
- **語意邊界(硬性)**:時序證明只進 check_const 的報告,**不得**餵給
  `tie_nets_const` / `const_propagate` 的變換 —— 變換側的 cec 驗證是
  flop-cut 組合等價,混用會炸 cec(硬性違規=零分)。`.latch` 模型忽略
  RN/SN 控制腳,與 run_random_sim/flop-cut 慣例一致,無新增健全性缺口。

**驗證紀錄**(全部通過):合成 fixture 三路(scleanup 單獨證出恆 0;
pdr 證出恆 0/恆 1 雙極性;負控制 q1 正確回反例);test31 n16 verdict
回歸不變;canary + test31 LLM 重跑 OK、`check_results` PASS、
`check_answers` 零 WRONG。最壞路徑 96.4s,守住 110s check_const 預算
(最終 SAT 對也被夾進預算)。TSD/SDD/agent-runtime 文件已同步。

## 3. 平行 session / worktree 狀態(`.claude/worktrees/`,07-12)

| worktree / 分支 | 任務 | 狀態 |
|---|---|---|
| `focused-robinson-f70898` | check_answers 提取誤報修復 | **已合併(`439450a`)**。selftest 過、原 5 個誤報全消(test20 兩題轉 UNVERIFIED 屬合理保守化)。worktree 可清(髒 `agent-runtime.md` 與已入庫版本逐位元組相同)。 |
| `pensive-leakey-a0740c` | 時序 BLIF + scleanup/pdr | **已合併(`53d5e48`)**(見 §2)。worktree 可清(未追蹤 `tools` 只是指回主 repo 的 symlink)。 |
| `intelligent-keller-e1eea2` | signal-equiv 證明工具 | 未動工。依賴(check_const、時序 BLIF)已全數合併 —— **rebase 到合併後基底即可開工**,scorr 可直接證 signal-equivalence classes(test20 turn 15 類題)。髒 `agent-runtime.md` 同 robinson,捨棄。 |
| `inspiring-shaw-dbe164` | successors-fix chip session | **已棄置刪除(07-12)**:未提交草稿是 robinson 修復的嚴格子集,零損失。 |

## 4. 待辦(優先序)

1. ~~合併 robinson + leakey~~ **完成(07-12)**:`439450a` + `53d5e48`,
   parser 重建、selftest、test02 金絲雀、check_results 40/40 全過,已推
   origin。shaw 已棄置刪除。
2. 清掉 robinson/leakey 兩個已合併 worktree(可選,零損失)。
3. keller session 開工:signal-equiv 工具(scorr 路線),基底 rebase 到
   合併後的 `integrate/eval-and-gates`。
4. ~~P2-9 cone 語意對齊~~ **完成(07-12 下午)**:詳見
   `docs/PLAN_QA_fixes.md` P2-9 收案註記。重點:C++ `--stop_at_dff` 預設
   組合語意;oracle 增 `drivers` 多值映射(dup-Q 網修正);check_answers
   仲裁兼查舊語意 + pio_count 認 bit 級;test38 path-disagree 假訊號排除。
5. ~~P2-6 floating-port 容錯~~ **完成(07-12 傍晚)**:見
   `docs/PLAN_QA_fixes.md` P2-6 收案註記與
   `tests/unit_tests/test_floating_ports.py`。P2-7 非確定性量測(可選)。
6. ~~收尾:全量 sweep + 四層驗證~~ **完成(07-12 晚)**:清掉全部 40 個
   舊 `testNN_out.v` 後從乾淨狀態起跑,四層全過 + 3 案 ×3 輪穩定性
   重跑。詳見 §7。

## 5. 本輪新知(細節見專案記憶與 chip 描述)

- ABC 1.01 指令齊全:`pdr`/`scleanup`/`scorr`/`sec`/`dsec`/`fraig`/`dch`
  皆實測可用;ABC 讀不了我們的 Verilog(named-pin dff)。**橋接缺口已由
  `write_seq_blif` 補上(9e94514)**;`scorr`(signal-equiv)與
  `sec`/`dsec`(可解 flop-cut D6 限制)是下一批可接的引擎。
- pdr verdict 字串:`Property proved.` /
  `Output 0 of miter "…" was asserted in frame N.`(frame 0 = 組合反例)/
  逾時 `Property UNDECIDED.`;`pdr -T <sec>` 有效。
- `run_all_llm.py` TIMEOUT 時 `0 tok/0 calls (0/20 turns)` 是報表歸零
  行為(run_all_llm.py:99),不代表沒啟動 —— 診斷時看 `<case>.log`
  的 `#END` 數。
- 機器高負載常態(多位同事跑重活),單案時間量測要在獨跑下做。

## 6. 快速指令

```bash
python3 scripts/build_parser.py                    # C++ 重建(合併 9e94514 後必要)
python3 scripts/run_all_llm.py --case 2            # 金絲雀(~15s)
python3 scripts/run_all_llm.py --case 39           # P1-8 最重用例(~8 min)
python3 scripts/check_results.py --all             # 硬性要求(期望 40/40)
python3 scripts/check_answers.py --case testNN --log-dir .   # 答案 vs oracle
```

## 7. 全量 sweep 收官結果(07-12 晚,gpt-4o-mini)

前置:parser 重建(P2-9 後兩個 commit 動過 C++)、`config.yaml` 確認
openai/gpt-4o-mini、**清掉全部 40 個舊 `testNN_out.v` + run log**
(staleness trap)、test02 金絲雀過(16.5s / 31.6k tok)。

| 驗證層 | 結果 |
|---|---|
| run_all_llm(全 40 案) | **40/40 OK**,63.7 min,6.53M tok / 1049 calls |
| check_results | **40/40 無硬性違規**,全數 EQUIVALENT;warn 皆為已知軟性項(D1/D2/test37 n8) |
| eval_harness | **40/40 PASS+EQUIV**,差分 gate-count 40/40 吻合 |
| check_answers | CORRECT=104、**WRONG=6**、DIVERGENT=1、UNVERIFIED=49、UNVERIFIED-TYPE=86、SKIPPED-STATE=31 |

**穩定性重跑**(test18/test20/test37 各 +2 次,共 3 輪;逐輪存檔
check_results + check_answers):

- **變換側完全確定**:6 次重跑全部 PASS+EQUIVALENT,每案三輪輸出
  gate 數一模一樣(test18=8538、test20=14371、test37=47232)。
  test37 雙 cone 重寫三輪皆過。
- **分析側兩類軟性損失**(皆非硬性違規,不歸零):
  1. **signal_equiv 空口宣稱** —— 6 筆 WRONG 全是 claim=True/
     oracle=False(test17–20)。test20 turn 17 三輪全 WRONG(確定性
     fabrication);test18 兩題在 WRONG ↔ 誠實 UNVERIFIED 間逐輪翻轉。
     log 顯示 agent 自建 miter 失敗("different number of primary
     inputs")後有時仍宣稱等價。**修法 = keller scorr 工具 + planner
     對此題型加 anti-fabrication 閘門**(目前閘門不涵蓋此 family,
     因為無 oracle 工具可對賭)。
  2. **check_answers 提取不足(checker 端低估,agent 其實答對)**:
     (a) 雙重否定句式 "There is no path ... that does not traverse"
     判 UNVERIFIED(test18/20 turn 4–5,三輪確定性);(b) 千分位逗號
     數字/清單尾巴的 cone-size 回答偶發 UNVERIFIED(test20 turn 13–14
     run2:claim 49/1,878 與 oracle 相符仍 UNVERIFIED)。已開背景任務
     修提取(補 selftest;沿 robinson e919a95 的保守原則,勿引回
     over-match)。
- test32 pio_count DIVERGENT 為已知 entries-vs-bits 定義差
  (claim=(57,244) 與 oracle bit 級一致),可忽略。
