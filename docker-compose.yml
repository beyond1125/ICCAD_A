services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    command: python main.py -config config.yaml
